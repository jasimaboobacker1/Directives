import { HomeDirectiveDirective } from './home-directive.directive';

describe('HomeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
